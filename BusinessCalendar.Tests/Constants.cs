namespace BusinessCalendar.Tests;

internal static class Constants
{
    public static readonly int CurrentYear = DateTime.Today.Year;
    public const string ShouldReferenceTheSameObject = "should reference the same object";
}